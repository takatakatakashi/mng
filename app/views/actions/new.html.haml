.post
  = form_with(model: @action, local: true) do |f|
    投稿する
    %p
    %input.post__message
    .post__mask
      %label.post__mask__image
      = fa_icon 'picture-o', class: 'icon'
      %input.hidden{type: 'file'}
      %input.post__submit{type: 'submit', value: '投稿する'}
-# .contents.row
-#   .container
-#     = form_with(model: @action, local: true) do |form|
-#       %h3
-#         投稿する
-#       = form.text_field :name, placeholder: "Nickname"
-#       = form.text_field :image, placeholder: "Image Url"
-#       = form.text_area :text, placeholder: "text" , rows: "10"
-#       = form.submit "SEND"
